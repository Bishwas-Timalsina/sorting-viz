export const sortingVisualizerDOM = document.getElementById(
  "sorting-visualizer-container__bar"
);

export const arrayRangeSliderDOM = document.querySelector(
  ".array-range-slider"
);

export const arraySizeTextDOM = document.querySelector(
  ".array-range-slider-label"
);

export const speedTextDOM = document.querySelector(".speed-range-slider-label");

export const sortingBar = document.getElementsByClassName("sorting-bar");

export const optionBtnDOM = document.querySelector(".sorting-option-btn");

export const sortingStartBtnDOM = document.querySelector(".start-sorting-btn");

export const generateNewArrayBtn = document.querySelector(
  ".generate-array-btn"
);

export const speedRangeSliderDOM = document.querySelector(
  ".speed-range-slider"
);

export const barDirectionBtn = document.querySelector(".bar-direction-btn");

export const myToast = document.querySelector(".my-toast");

export const SORTING_OPTION = ["Bubble Sort", "Selection Sort", "Merge Sort"];
export const COLORS = {
  bar: "orange",
  sortedNumber: "#9b59b6",
  firstNumber: "#FF4949",
  secondNumber: "#2ecc71",
};
